-- vim 选项配置 (vim.opt / vim.g)
-- 注意: 大部分默认值已由 AstroNvim 内部设置 (astronvim/plugins/_astrocore_options.lua)
return {
  opt = {
    clipboard = "unnamedplus", -- 使用系统剪贴板（与 AstroNvim 默认值一致，此处显式声明）
  },
  g = {}, -- vim.g 全局变量
}
